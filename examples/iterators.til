import std.io as io
import posix.shell as sh

proc ls () {
    return a b c
}

# CHANGE IT!
foreach (fname) ([ls]) {
    io.out $fname
}

foreach (filename) ([sh.ls "/usr/lib64"]) {
    $filename |> io.out
}

ls |> io.out
ls |> io.err
{1 2 3} |> io.out
